const libraryProvider = require("./library");
const bookProvider = require("./book");
const userProvider = require("./user");


module.exports = {
    bookProvider,
    libraryProvider,
    userProvider
}